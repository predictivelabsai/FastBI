FROM neo4j:5.26-community

EXPOSE 7474 7687

HEALTHCHECK --interval=15s --timeout=8s --start-period=45s --retries=10 \
  CMD cypher-shell -u "${NEO4J_USER:-neo4j}" -p "${NEO4J_PASSWORD}" "RETURN 1" >/dev/null 2>&1 || exit 1
