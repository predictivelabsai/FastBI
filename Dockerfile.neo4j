FROM neo4j:5.26-community

EXPOSE 7474 7687

# Coolify keeps this resource private and FastBI verifies it over Bolt. Avoid a
# container-level auth probe here: password interpolation differs between local
# Compose and Coolify's literal secret environment handling.
HEALTHCHECK NONE
